<h1>Add a Recipe to "<%= @category.name %>"</h1>

<%= simple_form_for [@category, @bookmark] do |f| %>
  <%= f.association :recipe, collection: @recipes %>
  <%= f.input :comment %>
  <%= f.button :submit, "Add Bookmark" %>
<% end %>

<%= link_to "Back to Category", category_path(@category) %>
